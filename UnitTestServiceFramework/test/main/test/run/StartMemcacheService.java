package test.run;

import service.framework.bootstrap.ClientBootStrap;

public class StartMemcacheService {
	 public static void main(String[] args) {
		// start memcache then regist the service information to the service center
//		ClientBootStrap clientBootStrap = new ClientBootStrap("conf/memecache_starter_client.properties", 5);
//		clientBootStrap.run();
//		clientBootStrap.getConsumerBean().prcessRequestPerConnect("", args);
	 }
}
